[package]
name = "pokerus-core"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = { version = "0.8.5", features = ["nightly", "log"] }
test-log = { workspace = true }
log = { workspace = true }
